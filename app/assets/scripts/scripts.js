(function () {
    'use strict';

    // Dependencies
    var $               = require('jquery'),
        bootstrap       = require('bootstrap');

        require('jqtouch');
        require('btselect');
        require('btswitch');
        require('fuicheckbox');
        require('fuiradio');
        require('jqinput');
        require('jqplaceholder');
        require('fuiapplication');


        // Start all events here
        function init () {
            // Here below all inits you need
        }

        $('document').ready(function () {
            // Init all things
            init();
        });
}());
